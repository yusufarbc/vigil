apiVersion: v1
kind: ServiceAccount
metadata:
  name: detection-service-sa
  namespace: sentinel
  annotations:
    iam.gke.io/gcp-service-account: sentinel-detection@${GCP_PROJECT}.iam.gserviceaccount.com
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: detection-service
  namespace: sentinel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: detection-service
  template:
    metadata:
      labels:
        app: detection-service
    spec:
      serviceAccountName: detection-service-sa
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: detection-service
          image: us-central1-docker.pkg.dev/${GCP_PROJECT}/sentinel/detection-service:${IMAGE_TAG}
          envFrom:
            - configMapRef:
                name: sentinel-config
          env:
            - name: ELASTIC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sentinel-es-elastic-user
                  key: elastic
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          volumeMounts:
            - name: es-certs
              mountPath: /etc/sentinel/certs
              readOnly: true
      volumes:
        - name: es-certs
          secret:
            secretName: sentinel-es-http-certs-public
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: detection-service-hpa
  namespace: sentinel
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: detection-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
